doctype html
html(lang="en")
    head
        tittle Update plant info
        link(rel="stylesheet", href="/css/style.css")
    body
        .form-wrapper
            p Plant: #{plantName}
            p Plant Color: #{plantColor}
            form(method="POST")
                label(for="newVal") New color 
                input(type="text" name="newVal" required)
                input(type="submit" value="Submit")